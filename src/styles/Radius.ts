import { moderateScale } from './responsiveStyles';

const Radius = {
  xs: moderateScale(4),
  sm: moderateScale(6),
  md: moderateScale(10),
  lg: moderateScale(14),
  xl: moderateScale(18),
  full: 999,
};

export default Radius;
